Create the file /etc/motd containing EXACTLY these two lines, in this order:

Authorized access only.
Contact ops@example.com

There must be no extra lines, leading spaces, or trailing text.
Each line ends with a normal newline.
