The `chronyd` service has been disabled and stopped.

Make sure `chronyd` is both:

  • running right now, and
  • configured to start automatically at every boot.

The service must come back up on its own after a reboot (do not rely on
starting it by hand).
