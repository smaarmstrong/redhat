Create a local user account with these exact attributes:

  • Username:        dbuser
  • UID:             1500
  • Comment (GECOS): Database User
  • Login shell:     /bin/bash

The account must still exist after a reboot (a normal `useradd` is fine).
