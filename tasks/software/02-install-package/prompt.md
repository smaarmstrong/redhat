Install the package:

  tree

It is part of the Rocky Linux BaseOS repository (available on the
installation DVD and from the CDN/mirrors), so any working repository will
provide it.

Once installed, the package stays installed across reboots. Only the
presence of the RPM is graded.
