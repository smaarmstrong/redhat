Back up a directory into a compressed archive.

  • The directory to archive is /opt/logs (it contains a few log files).
  • Create a gzip-compressed tar archive at /root/logs.tar.gz.
  • The archive must contain the /opt/logs directory and its files.

This is a one-off deliverable: the archive file simply needs to exist
on disk when grading runs.
