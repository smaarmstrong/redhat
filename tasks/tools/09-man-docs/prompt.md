Use the manual-page keyword search to find every command whose man-page
description mentions the word "password".

Save the FULL output of that search to:

  /root/pw-cmds.txt

The keyword search is done with `apropos password` (equivalently
`man -k password`). Redirect its complete output — one line per matching
command — into the file above.

Your saved file must match exactly what `apropos password` prints on this
system.
