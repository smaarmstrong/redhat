This system is currently set to boot into the graphical target.

Change the **default** systemd target so the machine boots into
**multi-user.target** (text/multi-user mode) automatically from now on.

The change must persist across reboots.
