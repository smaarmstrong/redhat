Read chapter 4, section 4.4.3 "ls command" (pp. 44-45): the main options
(-a, -i, -l) and the wider set (-d, -h, -R, -F, -A, -s), plus what the
columns of 'ls -lia' mean.

Type the exact command where asked (matching ignores case and extra
spaces); interpretation questions are multiple choice.
