Read chapter 4 "Commands for Linux Users", section 4.1 "Generalities"
including "The users" and "The shell" (pp. 28-31).

This quiz covers why the command line matters, how a user is defined, and
the shape of a Linux command. It is prose knowledge, so the questions are
multiple choice.
