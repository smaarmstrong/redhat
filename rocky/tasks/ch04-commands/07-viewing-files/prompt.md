Read chapter 4, section 4.5 "Visualization" up to the tail command
(pp. 50-53): file, more, less, cat (and -n, -b), tac, head and tail
(and -n, -f).

Type the exact command where asked (matching ignores case and extra
spaces); behaviour questions are multiple choice.
