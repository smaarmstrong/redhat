Read chapter 4, section 4.7 "Redirects and pipes" (pp. 60-64): the three
standard streams (stdin=0, stdout=1, stderr=2), input redirection (< and <<),
output redirection (> and >>, and 2> for errors), combining streams (2>&1),
/dev/null, and the pipe |.

Type the exact command/operator where asked (matching ignores case and
extra spaces); concept questions are multiple choice.
