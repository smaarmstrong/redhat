Read chapter 4, sections 4.4.4 to 4.4.9 (pp. 45-50): mkdir, touch, rmdir,
rm, mv and cp - creating, deleting, moving and copying files and directories.

Type the exact command where asked (matching ignores case and extra spaces);
behaviour questions are multiple choice. Remember that on Rocky, rm is
aliased to 'rm -i'.
