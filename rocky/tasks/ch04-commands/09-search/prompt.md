Read chapter 4, section 4.6 "Search" (pp. 57-60): find (and -name, -type,
-size, -exec), whereis, grep (and -i, -v, -w, ^, $, -R), and the
meta-characters (wildcards) *, ? and [ ].

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice. Note the guide's warning: shell
wildcards are NOT the same as grep's regular-expression meta-characters.
