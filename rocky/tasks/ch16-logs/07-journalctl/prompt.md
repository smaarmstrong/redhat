Read chapter 16, section 16.3.1 "journalctl command" (pp. 316-318): the
journalctl options for filtering by unit (-u), kernel (-k), boot (-b), time
(--since/--until), count (-n), priority (-p), following (-f), pattern (-g),
and managing disk usage (--disk-usage, --vacuum-size).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
