Read chapter 16, section 16.1 "Overview" and the start of 16.2 "rsyslog"
(pp. 302-303): what a log is, the two collectors (rsyslog and journald), and
the key files under /var/log/ - including which are binary and need a special
command to read.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
