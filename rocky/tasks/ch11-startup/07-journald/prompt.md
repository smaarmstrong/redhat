Read chapter 11, sections 11.3.4 "The journald process" and 11.3.5
"journalctl command" (pp. 207-209): what journald captures and how it stores
logs, and using journalctl to view, follow (-f) and filter by priority (-p).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
