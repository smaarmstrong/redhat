Read chapter 11, section 11.3.3 "Using system targets" (pp. 206-207): how
targets replace SysV run levels, the common targets, getting/setting the
default target, isolating a target, and the rescue vs emergency modes.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
