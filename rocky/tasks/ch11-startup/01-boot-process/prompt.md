Read chapter 11, section 11.1 "The boot process" (pp. 194-197): the boot
stages (BIOS/POST, MBR, GRUB2, kernel, systemd), the GRUB2 configuration
files, and how grub2-mkconfig regenerates grub.cfg.

Type the exact command where asked - the guide runs these as root (matching
ignores case and extra spaces). Concept questions are multiple choice.
