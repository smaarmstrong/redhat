Read chapter 13, sections 13.4 (hostname), 13.5 (DNS files) and 13.6
(nsswitch/getent) (pp. 244-251): setting the hostname with hostnamectl, the
name-resolution order, and the /etc/hostname, /etc/hosts, /etc/resolv.conf and
/etc/nsswitch.conf files plus getent.

Type the exact command where asked - the guide runs these as root (matching
ignores case and extra spaces). Concept questions are multiple choice.
