Read chapter 13, section 13.1.4 "DNS" (pp. 234-235): what DNS is for, the
difference between a hostname, a domain and an FQDN, the domain hierarchy
(root, TLD, second-level), the root servers, and the meaning of a zone.

These are concepts, so the questions are multiple choice.
