Read chapter 13, section 13.1.5 "ISO/OSI 7-layer theoretical model"
(pp. 236-237): the seven OSI layers (and the memory aid), and the TCP/IP
4-layer model - its layers, example protocols, and the hardware that works at
each layer.

These are concepts, so the questions are multiple choice.
