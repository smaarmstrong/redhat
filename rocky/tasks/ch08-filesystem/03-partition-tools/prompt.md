Read chapter 8, the partitioning tools in sections 8.1.2 to 8.1.4
(pp. 128-130): fdisk (and its limits), cfdisk, parted and gparted, plus the
five steps of preparing physical media without LVM.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
