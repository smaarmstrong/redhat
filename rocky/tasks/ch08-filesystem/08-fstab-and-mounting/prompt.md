Read chapter 8, sections 8.4.1 "/etc/fstab file" and 8.4.2 "Mount management
commands" (pp. 143-145): the six columns of /etc/fstab, mount -a and
/etc/mtab, and the mount and umount commands with their options.

Type the exact command where asked - the guide runs these as root
(matching ignores case and extra spaces). Concept questions are multiple
choice.
