Read chapter 8, section 8.3 "Structure of a file system" (pp. 137-141):
what a file system does, creating one with mkfs, the boot sector
(MBR/DPT/BRID), the super block, the inode table, the data block, the role
of sync, and repairing with fsck.

Type the exact command where asked - the guide runs these as root
(matching ignores case and extra spaces). Concept questions are multiple
choice.
