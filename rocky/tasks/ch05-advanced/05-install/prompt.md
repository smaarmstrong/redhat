Read chapter 5, section 5.6 "install command" (pp. 78-79): what install
really does (despite its name), and the -d, -D, -t, -m, -o/-g, -p, -b and -S
options.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
