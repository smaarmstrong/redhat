Read chapter 5, section 5.1 "uniq command" (pp. 71-72): how uniq removes
duplicate lines, why it must be paired with sort, and the -u, -d, -D and -c
options.

The examples use a file firstnames.txt. Type the exact command where asked
(matching ignores case and extra spaces); concept questions are multiple
choice.
