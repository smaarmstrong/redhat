Read chapter 5, sections 5.7 "tree command" and 5.8 "stat command"
(pp. 80-82): the tree options (-a, -h, -u, -g, -p) and the fields stat
reports, especially the atime / mtime / ctime / Birth timestamps and the
block-size caveat.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
