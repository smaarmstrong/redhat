Read chapter 5, section 5.2 "xargs command" (pp. 72-74): how xargs builds and
runs command lines from standard input, its default command, ending input
with Ctrl+D, where it places the arguments, and the -L, -n and -I options.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
