Read chapter 5, sections 5.4 "psmisc packages" and 5.5 "watch command"
(pp. 76-77): the pstree, killall and fuser utilities from psmisc, and the
watch command with its -n and -t options.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
