Read chapter 9, section 9.2 "Viewing processes" (pp. 158-159): the ps command
and its options (-e, -f, -u, -p, -H), its default behaviour, and the meaning
of the output columns (UID, PID, PPID, C, STIME, TTY, TIME, CMD).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
