Read chapter 9, sections 9.6.9 to 9.6.11 (pp. 167-169): finding processes by
name with pgrep, signalling them by name with pkill and killall (default
signal SIGTERM), and viewing the process tree with pstree.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
