Read chapter 15, section 15.3.4 "Sticky BIT" (pp. 295-297): what the sticky
bit does on a directory (the /tmp example), how to set it (the value 1,
chmod o+t), and that root is not restricted by it.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
