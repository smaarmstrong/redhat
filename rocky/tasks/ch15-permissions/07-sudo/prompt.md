Read chapter 15, section 15.3.6 "sudo" (pp. 299-301): what sudo is for,
editing /etc/sudoers with visudo, the sudoers line syntax
(user MACHINE=(runas) COMMANDS), listing your rights with sudo -l, and
checking syntax with visudo -c.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
