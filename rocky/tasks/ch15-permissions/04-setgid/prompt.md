Read chapter 15, section 15.3.3 "SetGID" (pp. 293-295): what SetGID does on
executables (the locate example) and on directories (group inheritance), how
to set it (the value 2, chmod g+s), and finding SGID files.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
