Read chapter 15, section 15.3.1 "ACL permissions" (pp. 285-291): why ACLs
exist, viewing them with getfacl, setting them with setfacl (-m, -x, -b, -d,
-R), the meaning of the mask, and default/recursive ACLs.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
