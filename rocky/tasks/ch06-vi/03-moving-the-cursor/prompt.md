Read chapter 6, section 6.3 "Moving the cursor" (pp. 87-88): moving by
character (h j k l, 0, $), by word (w, b) and around the file/screen
(gg, G, nG, H, M, L). All of these are typed in command mode.

Several keys differ only by CASE, so most questions here are multiple choice
to keep the distinction sharp; where a command is unambiguous you type it
(matching ignores case and extra spaces).
