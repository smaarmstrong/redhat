Read chapter 6, section 6.6 "EX commands" (pp. 92-95): line numbering
(:set nu), searching (/string, ?string, n, N) and its regex characters,
substitution (:%s/search/replace, ranges, the /g flag), and deleting lines
with :g patterns.

Type the ex-mode commands exactly, including the leading colon or slash
(matching ignores case and extra spaces). Concept questions are multiple
choice.
