Read chapter 7, section 7.4 "File owners" (pp. 112-113): changing a file's
owner and group with chown (its user, :group, user:group and user: forms,
plus -R), changing only the group with chgrp, and copying ownership from
another file with --reference.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
