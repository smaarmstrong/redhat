Read chapter 7, sections 7.3.1 "Definition" and 7.3.2 "useradd command"
(pp. 104-107): the useradd options (-u, -g, -G, -d, -s, -c, -M, -r), what
useradd sets by default, the locked/no-password state at creation, the
skeleton copy from /etc/skel, and setting defaults with useradd -D.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
