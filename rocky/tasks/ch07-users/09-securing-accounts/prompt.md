Read chapter 7, section 7.6 "Securing" (pp. 116-118): managing passwords with
passwd (-l, -u, -d, -S, -e, and the ageing options -n/-x/-w/-i, plus
--stdin), and password-ageing with chage (-l, -m, -M, -W, -E, -I, and
chage -d 0).

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
