Read chapter 7, sections 7.7 "Advanced management" and 7.8 "Identity change"
(pp. 119-124): the /etc/default/useradd and /etc/login.defs settings, the
/etc/skel template directory, and switching identity with su - including the
important difference between 'su' and 'su -'.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
