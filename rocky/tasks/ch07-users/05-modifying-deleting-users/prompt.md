Read chapter 7, sections 7.3.3 "usermod command" and 7.3.4 "userdel command"
(pp. 108-109): modifying a user (-u, -l, -d/-m, -L/-U, -e) and especially the
crucial difference between -G and -aG for supplementary groups, plus deleting
a user with userdel -r.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
