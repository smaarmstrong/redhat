Read chapter 7, sections 7.2.1 to 7.2.3 (pp. 99-101): groupadd (-g, -f, -r),
groupmod (-g, -n) and groupdel, plus what happens to a group's files after
its GID changes.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
