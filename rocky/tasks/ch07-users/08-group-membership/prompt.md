Read chapter 7, section 7.5 "Guest management" (pp. 114-115): managing group
membership with gpasswd (-a, -A, -d, -M), viewing a user's groups with id,
and switching primary group temporarily with newgrp.

Type the exact command where asked - include sudo where the guide uses it
(matching ignores case and extra spaces). Concept questions are multiple
choice.
