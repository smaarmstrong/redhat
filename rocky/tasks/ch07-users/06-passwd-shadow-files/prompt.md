Read chapter 7, sections 7.3.5 "/etc/passwd file" and 7.3.6 "/etc/shadow
file" (pp. 109-111): the seven colon-separated fields of /etc/passwd and the
security fields of /etc/shadow (encrypted password, password ageing, account
expiry).

These are file-format concepts, so the questions are multiple choice.
