Read chapter 14, section 14.3.2 "Query package" (pp. 258-259): the rpm query
options - -q with -a, -i, -l, -f, -p, -R and --last - and the note that you
cannot use wildcards (use grep instead).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
