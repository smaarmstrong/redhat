Read chapter 14, sections 14.3 "Local package manager" and 14.3.1 (pp.
256-257): the rpm command, the difference between a Full Package Name and a
Package Name, the RPM database location, the install/upgrade/uninstall options
(-i, -U, -e, -h, -v, --test), and RPM's manual dependency handling.

Type the exact command where asked - the guide runs these as root (matching
ignores case and extra spaces). Concept questions are multiple choice.
