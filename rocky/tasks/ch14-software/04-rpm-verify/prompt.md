Read chapter 14, sections 14.3.3 "Verify the signature" and 14.3.4 "Verify
file changes" (pp. 259-261): checking a package's signature with rpm -K, and
checking whether installed files changed with rpm -V (the 9-character first
column and the file-type second column).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
