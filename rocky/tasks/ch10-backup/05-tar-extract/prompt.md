Read chapter 10, the tar extraction subsections (pp. 183-185): extracting
with xvf (and xvfz / xvfj / xvfP), pulling out a single file or directory, and
matching a group of files with --wildcards.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
