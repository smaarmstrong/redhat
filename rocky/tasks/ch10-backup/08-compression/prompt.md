Read chapter 10, section 10.4 "Compression - decompression utilities"
(pp. 191-193): compressing with gzip and bzip2, decompressing with gunzip and
bunzip2, the resulting extensions, and why it is better to back up then
compress rather than compress during the backup.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
