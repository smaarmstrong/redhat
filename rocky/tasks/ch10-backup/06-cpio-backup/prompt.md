Read chapter 10, section 10.3 "CoPy Input Output - cpio" and 10.3.1 "copy-out
mode" (pp. 186-188): cpio's three modes, how copy-out (-o) takes a file list
piped from find/ls/cat, the -F option, relative vs absolute paths, and
appending with -A.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
