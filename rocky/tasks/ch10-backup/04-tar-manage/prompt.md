Read chapter 10, the tar subsections on adding, listing and checking
(pp. 181-183): appending items with r/A, listing contents with t, and
verifying integrity with W (at creation) or d (after creation).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
