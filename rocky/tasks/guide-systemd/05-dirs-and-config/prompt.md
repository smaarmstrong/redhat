Read the "Important directories" and "systemd configuration files" sections of
the About systemd guide: the three unit-file directories and their priority
order, and the system.conf configuration file.

These are concepts, so the questions are multiple choice.
