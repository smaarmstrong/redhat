Read the "systemd units file content description" section of the About systemd
guide, covering the [Unit] title (Description, After/Before, Requires vs
Wants, Conflicts) and the [Service] title (the Type= values, ExecStart/
ExecReload/ExecStop, Restart=, KillMode=).

These are concepts, so the questions are multiple choice.
