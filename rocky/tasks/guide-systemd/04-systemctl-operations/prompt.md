Read the "Operational units" demonstrations in the About systemd guide: the
systemctl subcommands is-enabled, mask/unmask, cat, edit and show (beyond the
familiar start/stop/enable).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
