Read the "systemd as PID 1" section of the About systemd guide: how systemd
uses /etc/fstab and default.target, the mapping between old SysV runlevels and
systemd targets, and the sysinit.target / basic.target checkpoints.

These are concepts, so the questions are multiple choice.
