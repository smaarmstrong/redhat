Read the [Install] title description and "Command related to other components"
in the About systemd guide: WantedBy=/RequiredBy=, Alias=, Also=, and the
companion commands timedatectl, hostnamectl, localectl, systemd-analyze and
loginctl.

These are concepts, so the questions are multiple choice.
