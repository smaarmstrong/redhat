Read the "Unit types" list in the About systemd guide: the kinds of units
systemd manages (service, socket, device, mount, automount, swap, target,
path, timer, snapshot, slice, scope).

These are concepts, so the questions are multiple choice.
