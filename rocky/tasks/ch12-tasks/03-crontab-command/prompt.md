Read chapter 12, section 12.4 "Scheduling tasks" and "The crontab command"
(pp. 213-214): where a user's schedule file lives, the crontab options
(-e, -l, -r, -u), the caution about crontab with no options, and the
constraints on scheduled programs.

Type the exact command where asked - the guide runs these as root (matching
ignores case and extra spaces). Concept questions are multiple choice.
