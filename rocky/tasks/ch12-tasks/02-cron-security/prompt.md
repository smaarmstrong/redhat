Read chapter 12, section 12.3 "Security" (pp. 211-213): how /etc/cron.allow
and /etc/cron.deny control who may use cron, and the precedence rules between
them.

These are rules to reason about, so the questions are multiple choice.
