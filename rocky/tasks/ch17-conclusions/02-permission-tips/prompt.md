Read chapter 17's tips on permissions and groups: why you should never run
chmod -R 644 on a directory tree (and the safer alternatives), the setgid bit
for group inheritance, making a file immutable with chattr +i, and the
newgrp / gpasswd group tips.

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
