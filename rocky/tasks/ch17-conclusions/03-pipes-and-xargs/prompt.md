Read chapter 17's tips on the fact that a pipe only carries stdout (and the
2>&1 ordering subtlety), that tar uses keys rather than options, and turning a
list into arguments with xargs (-I{}, -P, and -0 with find -print0).

Type the exact command where asked (matching ignores case and extra spaces);
concept questions are multiple choice.
