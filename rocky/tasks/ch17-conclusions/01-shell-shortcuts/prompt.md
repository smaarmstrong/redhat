Read chapter 17 "Conclusions" - the tips on the end-of-options marker --,
jumping directories with cd -, the fact that [ is a real program, editing the
command line in your editor, brace expansion, and history shortcuts.

Type the exact command/keystroke where asked (matching ignores case and extra
spaces); concept questions are multiple choice.
