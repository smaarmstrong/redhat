Practice real edits in vi (vim). The file /opt/found/notes/journal.txt
needs two changes:

  • On the "Disk usage" line, change the word MONTHLY to weekly
    (so the line reads:  Disk usage is checked weekly.)
  • Add a new final line containing exactly:  Reviewed and approved.

Every other line must stay exactly as it is.

Do the edit in vi — the grader only checks the end state, but making
your fingers do it in the editor is the whole point of this exercise.
The practice directory is world-writable, so no sudo is needed.
