Work in /root/rhce/ansible-doc/. Using `ansible-doc`, look up the
`ansible.builtin.user` module and find the parameter that sets a user's login shell.

Write ONLY that parameter name (a single word, no quotes, no extra text) to the file
`/root/rhce/ansible-doc/answer.txt`.
