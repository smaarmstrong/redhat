Work in /opt/rhce/navigator/. Create an `ansible-navigator.yml` configuration file
that sets:

  • `ansible-navigator.mode` = `stdout`
  • `ansible-navigator.playbook-artifact.enable` = `false`

Write valid YAML with the nested structure shown by the dotted key paths above.
