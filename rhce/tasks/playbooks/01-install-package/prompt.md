Work in /opt/rhce/install-package/. Write `playbook.yml` that ensures the `tree`
package is installed on the hosts in the `managed` group, then run it with
`ansible-playbook`. Your playbook must be idempotent (a second run changes nothing).
